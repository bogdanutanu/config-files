set number

" Indentation without hard tabs, spaces instead
set expandtab
set shiftwidth=2
set softtabstop=2

" Copies the indentation from previous line
set autoindent
" Adds an extra level of indentation in some cases
set smartindent

" Highligh all search pattern matches
set hlsearch
